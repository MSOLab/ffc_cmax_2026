# 계획: subroutine 내부 시간% 기반 progression JSON 추가

> 날짜: 2026-04-05 19:46 +09:00
> 목표: 현재의 "subroutine별 1개 점" 계산/표시는 유지하면서, 각 subroutine 내부에서 시간%에 따라 RPDf 통계값이 어떻게 변하는지 볼 수 있도록 post-process용 JSON 산출물과 subroutine별 list 데이터를 추가한다.

## 확인된 현재 상태

- `hfs_multi_instance_runner.py`의 `post_run_process()`는 먼저 `create_method_end_time_and_obj_value_summary()`를 호출해 `summary_method_end_time_and_obj_value_long.csv`를 만든 뒤, `_create_rpd_summary()`로 `summary_method_rpdf_and_norm_time_long.csv`와 두 HTML/SVG를 생성한다.
- `hybridflowshop/report/log_processor.py`는 이미 `subroutine_controller.log`에서 각 subroutine의 `start_sec`, `elapsed_sec`, `end_sec`를 파싱하고, per-instance `method_time_log.json`도 쓴다.
- 같은 `log_processor.py`는 `<instance>/results/<instance>_obj_log.yaml`에서 전체 objective 시계열(`obj_value.data`)과 note(`obj_value.notes`)를 읽는다.
- 현재 summary CSV는 subroutine마다 endpoint 1개만 남기므로, subroutine 내부에서 초반/후반 어느 시점에 개선이 몰리는지는 이미 손실된다.
- 반면 raw artifact에는 필요한 핵심 정보가 이미 있다.
  - subroutine window: `subroutine_controller.log`
  - global objective trace: `*_obj_log.yaml`
  - reference objective / job_cnt / stage_cnt: baseline CSV
  - timelimit: `runner.stopping_criteria.timelimit`

## 사용자 방향 반영 메모

- 2026-04-05 기준으로 설계를 수정한다.
- 기존 계획은 post-process에서 raw log를 재구성해 scenario-level JSON을 만드는 방향이었지만, 이제는 `SubroutineController`가 실행 중에 subroutine별 progression data를 직접 관리하는 방향을 1순위 source-of-truth로 삼는다.
- 따라서 신규 JSON의 1차 저장 단위는 scenario-level이 아니라 per-instance가 된다.
- Multi-scenario 후처리는 이 per-instance JSON들을 읽어 집계하는 방식으로 바꾼다.

## 이번 단계의 범위

### 포함

- `SubroutineController` 책임으로 subroutine call별 `(time, value)` list 수집
- 각 subroutine call의 local time 원점을 `0`으로 맞춘 list 생성
- controller 차원에서 모든 subroutine call list를 합친 combined list 생성
- controller 차원에서 `(subroutine 종료 시각, prefix가 붙은 subroutine 이름)` marker list 생성
- `SingleInstanceRunner.post_run_process()`에서 신규 per-instance JSON 저장
- `MultiScenarioRunner`와 chart/report 로직이 이 JSON들을 읽어 후처리하도록 설계 변경
- 기존 subroutine별 one-point scatter 좌표 유지
- 관련 테스트 추가

### 제외

- 기존 CSV 파일명/컬럼명/의미 변경
- 기존 `obj_log.yaml`, `method_time_log.json` 산출물 제거
- objective bound 중심 확장
- 기존 HTML의 점 위치 계산 공식 변경

## 설계 원칙

- 신규 JSON의 source-of-truth는 controller가 실행 중 기록한 in-memory structure다.
- 저장되는 기본 값은 우선 `(time, obj_value)`이며, `RPDf`는 후처리 단계에서 baseline과 결합해 계산한다.
- 동일 subroutine이 여러 번 호출될 수 있으므로 key는 `subroutine_name` 단독이 아니라 `call_index` prefix가 포함된 이름을 사용한다.
- local view의 시간축은 항상 subroutine 시작 시점을 `0`으로 둔다.
- 기존 scatter 점은 endpoint 기준 평균점으로 유지하고, 새 JSON은 그 점의 내부 shape를 설명하는 부가 정보로 사용한다.
- backward compatibility를 위해 기존 CSV 파이프라인은 유지하되, JSON 기반 집계를 점진적으로 우선화한다.

## 제안 산출물

### 1. Controller in-memory data

`SubroutineController`가 run 중 유지할 구조는 최소 다음 3개다.

- `subroutine_call_progress_map`
  - key: `prefixed_subroutine_name` 예: `3-repeat_while_improvement`
  - value: local time 기준 `(time, value)` list
- `combined_progress_list`
  - 모든 subroutine call의 point를 global execution order로 합친 list
- `subroutine_end_marker_list`
  - `(global_end_time, prefixed_subroutine_name)` list

권장 필드 확장:

- `call_index`
- `subroutine_name`
- `global_start_sec`
- `global_end_sec`
- `elapsed_sec`

### 2. 신규 per-instance JSON

- 파일명 제안: `subroutine_progression.json`
- 저장 위치 제안: 각 instance working dir 또는 `results/` 하위
- 생성 시점: `HfsSingleInstanceRunner.post_run_process()`

최상위 예시:

```json
{
  "artifact_version": 1,
  "instance_id": "1",
  "timelimit_sec": 50.0,
  "subroutine_calls": [],
  "combined_progress_list": [],
  "subroutine_end_marker_list": []
}
```

### 3. `subroutine_calls` entry 구조

```json
{
  "call_index": 3,
  "subroutine_name": "repeat_while_improvement",
  "prefixed_subroutine_name": "3-repeat_while_improvement",
  "global_start_sec": 12.0,
  "global_end_sec": 31.0,
  "elapsed_sec": 19.0,
  "local_progress_list": [
    {"local_sec": 0.0, "obj_value": 1320.0},
    {"local_sec": 4.2, "obj_value": 1280.0},
    {"local_sec": 19.0, "obj_value": 1250.0}
  ]
}
```

### 4. `combined_progress_list` 구조

```json
[
  {
    "global_sec": 12.0,
    "obj_value": 1320.0,
    "call_index": 3,
    "prefixed_subroutine_name": "3-repeat_while_improvement",
    "local_sec": 0.0
  }
]
```

### 5. `subroutine_end_marker_list` 구조

```json
[
  {
    "global_end_sec": 31.0,
    "call_index": 3,
    "prefixed_subroutine_name": "3-repeat_while_improvement",
    "subroutine_name": "repeat_while_improvement"
  }
]
```

- JSON은 확장 가능성을 위해 dict/list 기반으로 열어두고, 이후 `job_cnt`, `stage_cnt`, `ref_obj_value`, `rpd_f` 파생값을 추가해도 되도록 한다.

## 핵심 계산 및 책임 분리

### 1. `SubroutineController` 책임

- subroutine call 시작 시각 기록
- subroutine call 종료 시각 기록
- active subroutine call 동안 발생한 objective value point를 local time 기준으로 적재
- 종료 시 `subroutine_end_marker_list`에 marker 추가
- 전체 실행 순서 기준 `combined_progress_list` 동기화

실행 시점 hook의 기본 방향:

- `controller_core.py`의 `run()` 루프에서 `_run_flow(subroutine_data)` 호출 전후로 call 시작/종료를 감싼다.
- objective point 추가는 `add_obj_value_log()` / `extend_obj_value_log()` 계열 호출을 controller 쪽에서 함께 감싸거나 mirror 기록한다.

### 2. time 축 정의

subroutine call 내부 point의 local time은

$$
t_{local} = t_{global} - t_{subroutine,start}
$$

으로 정의한다.

후처리 단계에서 local ratio가 필요하면

$$
u = \frac{t_{local}}{t_{subroutine,end} - t_{subroutine,start}}
$$

로 파생 계산한다.

### 3. point 값 정의

- controller가 저장하는 기본 point value는 `obj_value`
- baseline reference와 timelimit를 알 수 있는 상위 후처리 단계에서 다음 값을 파생 계산한다.
  - `norm_time`
  - `rpd_f`
  - `local_ratio`

### 4. 평균 점 유지

- 기존 scatter용 점은 현재와 동일하게 subroutine endpoint 기준으로 계산한다.
- 즉 `summary_method_rpdf_and_norm_time_long.csv` 기반의 `mean_norm_time`, `mean_rpd_f` 정의는 유지한다.
- 새 JSON은 점 위치를 바꾸기 위한 것이 아니라, 점에 대응되는 내부 progression 통계를 추가하기 위한 것이다.

## 구현 단계

### 1. Controller progression recorder 추가

대상: `hybridflowshop/controller/controller_core.py` 및 필요 시 `hybridflowshop/controller/hfs_cp_lns.py`

- controller state에 다음 필드를 추가한다.
  - active call metadata
  - `subroutine_call_progress_map`
  - `subroutine_call_list`
  - `combined_progress_list`
  - `subroutine_end_marker_list`
- `run()` 루프에서 각 `_run_flow()` 호출 전후로 start/end bookkeeping을 추가한다.
- `prefixed_subroutine_name`은 명시적 `call_index` 기준으로 생성한다.
- 동일 이름 subroutine 반복 호출이 있어도 고유 key가 유지되게 한다.

### 2. objective point mirror 기록 추가

대상: controller의 objective logging 경로

- 현재 `add_obj_value_log()` / `extend_obj_value_log()`가 불리는 지점에서 active subroutine call에 해당하는 local point도 함께 기록한다.
- 가능하면 per-method 흩어진 호출마다 수작업을 넣지 말고 controller 공통 wrapper/helper에서 처리한다.
- point는 기본적으로 `(global_sec, obj_value)`에서 시작해, JSON 직렬화 전 `local_sec`도 포함한다.

### 3. SingleInstanceRunner JSON 저장 추가

대상: `hfs_single_instance_runner.py`

- `post_run_process()`에서 controller가 모은 progression structure를 신규 JSON으로 저장한다.
- 저장 helper 예시:
  - `save_subroutine_progression_json()`
- 파일 경로는 `self.working_dir` 또는 `self.result_dir` 하위로 일관되게 정한다.
- 이 단계에서는 JSON 저장만 담당하고, scenario 집계는 하지 않는다.

### 4. scenario 집계 helper 추가

대상: `hybridflowshop/report/` 하위 신규 모듈 제안 `method_progression_report.py`

- 이 모듈은 per-instance JSON들을 읽고 baseline/timelimit와 결합해 chart용 집계 payload를 만든다.
- 주요 책임
  - per-instance JSON load
  - endpoint 기준 `norm_time`, `rpd_f` 계산
  - subroutine별 mean point 계산
  - local ratio grid 위의 mean progression curve 계산
  - improvement curve 계산

### 5. Scenario/Multi-scenario 후처리 연동

대상: `hfs_multi_instance_runner.py`, `hfs_multi_scenario_runner.py`

- `HfsMultiInstanceRunner`는 자기 scenario 안의 instance JSON들을 읽어 scenario-level chart 입력으로 사용한다.
- `HfsMultiScenarioRunner`는 각 scenario dir 아래의 instance JSON들을 읽어 top-level 비교용 payload를 만든다.
- 사용자가 제안한 방향대로, 상위 후처리는 저장된 JSON 파일을 읽는 방식으로 통일한다.
- practical note: 현재 scenario HTML 생성 위치가 `HfsMultiInstanceRunner`이므로, 이 부분은 같은 JSON source를 읽도록 바꾸는 것이 호환성 측면에서 안전하다.

### 6. HTML/CLI 연동

대상:

- `hybridflowshop/report/method_summary_chart.py`
- `hybridflowshop/report/multi_scenario_method_chart.py`
- `scripts/plot_selected_subroutine_flow_comparison.py`

변경 방향:

- 메인 scatter 점은 유지
- 새 JSON 집계로부터 subroutine 내부 평균 progression curve를 detail plot 또는 interaction으로 추가
- selected scenario script도 JSON 우선, 기존 CSV fallback 구조로 맞춘다.

## 테스트 계획

### 1. controller recorder 테스트

- 신규 파일 제안: `tests/controller/test_subroutine_progression_recorder.py`
- 케이스
  - subroutine 시작/종료 시각이 기록됨
  - repeated subroutine name이 있어도 `call_index` prefix로 분리됨
  - active subroutine 동안 추가된 objective point가 local time 기준으로 저장됨
  - `combined_progress_list`와 `subroutine_end_marker_list`가 올바른 순서를 가짐

### 2. single-instance JSON 저장 테스트

- 대상: `tests` 또는 `tests/controller`/`tests/report`
- 케이스
  - `HfsSingleInstanceRunner.post_run_process()`가 신규 JSON을 저장함
  - JSON에 `subroutine_calls`, `combined_progress_list`, `subroutine_end_marker_list`가 포함됨
  - JSON schema가 최소 필드를 만족함

### 3. scenario aggregation 테스트

- 신규 파일 제안: `tests/test_method_progression_report.py`
- 케이스
  - per-instance JSON 여러 개를 읽어 subroutine mean point 계산
  - local ratio 평균 curve 계산
  - endpoint 점 위치가 기존 CSV 기반 값과 동일함
  - baseline/timelimit 누락 시 제외 규칙 검증

### 4. chart 회귀 테스트

- `tests/test_method_summary_chart.py`
  - 기존 점 위치 불변 확인
  - 새 payload/HTML에 progression detail data가 반영되는지 확인

- `tests/test_multi_scenario_timepoint_outputs.py`
  - multi-scenario 후처리가 instance JSON을 읽어 HTML을 생성하는지 확인
  - scenario label/subroutine label/축 범위가 유지되는지 확인

### 5. selected scenario script 테스트

- `tests/test_plot_selected_subroutine_flow_comparison.py`
  - JSON 우선 사용
  - JSON 없을 때 CSV fallback 유지

## 리스크 및 주의사항

- controller 공통 hook를 잘못 잡으면 subroutine 경계가 누락되거나 point가 중복 기록될 수 있다.
- `add_obj_value_log()`와 `extend_obj_value_log()` 호출 경로가 많으므로 per-method patch보다 공통 wrapper 방식이 중요하다.
- subroutine 내부에 objective update가 전혀 없는 경우에도 빈 list 대신 최소 anchor 정책이 필요한지 구현 전에 결정해야 한다.
- repeated subroutine name이 있으므로 prefix key 없이는 후처리 집계가 깨진다.
- scenario HTML은 현재 `HfsMultiInstanceRunner`가, top-level HTML은 `HfsMultiScenarioRunner`가 생성한다. 둘 다 동일 JSON source를 읽도록 맞추되, 역할 경계가 어긋나지 않게 주의해야 한다.
- 기존 CSV 파이프라인을 한 번에 제거하면 회귀 범위가 커진다. 초기 단계는 additive 방식이 안전하다.

## 검증 명령 계획

- `uv run pytest tests/controller/test_subroutine_progression_recorder.py tests/test_method_progression_report.py tests/test_method_summary_chart.py tests/test_multi_scenario_timepoint_outputs.py tests/test_plot_selected_subroutine_flow_comparison.py -q`
- `uv run ruff check hybridflowshop/controller hybridflowshop/report hfs_single_instance_runner.py hfs_multi_instance_runner.py hfs_multi_scenario_runner.py scripts/plot_selected_subroutine_flow_comparison.py tests`

## 완료 기준

- 각 instance dir에 신규 `subroutine_progression.json`이 생성된다.
- JSON에는 `subroutine_calls`, `combined_progress_list`, `subroutine_end_marker_list`가 포함된다.
- repeated subroutine name도 `call_index` prefix로 구분된다.
- 상위 후처리는 저장된 JSON 파일을 읽어 subroutine 내부 progression 통계를 계산한다.
- 기존 subroutine mean point 위치는 유지된다.
- scenario/multi-scenario HTML이 새 JSON 기반 progression detail을 표시할 수 있다.

---

## 변경 이력

### 2026-04-06: source-of-truth 재구성 버그 수정 + improvement curve sparse bucket 처리

#### 문제 1: `get_progression_data()`에서 metadata 재구성

- `global_start_sec`, `global_end_sec`, `elapsed_sec`를 marker/local_list에서 재구성했는데, marker가 없으면 `None`이 되고, local_list가 비면 elapsed가 붕괴됨
- **수정**: `_subroutine_call_meta_list`를 신규 필드로 추가. `_start_subroutine_call()`에서 call metadata를 즉시 저장하고, `_end_subroutine_call()`에서 `global_end_sec`, `elapsed_sec`를 채운다. `get_progression_data()`는 이 stored metadata를 그대로 serialize한다.
- 파일: `controller_core.py:88-89`, `:308-340`, `:360-401`

#### 문제 2: `compute_improvement_curve()` bucket 정렬 및 sparse 처리

- bucket이 정렬되지 않고 `iloc[0]/iloc[-1]`에 의존하며, 1개 row면 improvement가 인위적으로 0이 됨
- **수정**: `group.sort_values(["call_index", "global_sec"])`로 명시적 정렬, `len(bucket) < 2`면 skip
- 파일: `method_progression_report.py:159-165`

#### `repeat_while_improvement` 동작 확인

- `repeat_while_improvement`는 top-level call 하나로 기록됨
- 내부 `profile_fixed_ns`, `critical_job_ns`, `incremental_pw_cp`에서 발생한 objective update는 전부 바깥 `repeat_while_improvement` call의 `local_progress_list`에 시간순으로 쌓임
- 따라서 `subroutine_progression.json`에서 `repeat_while_improvement` 항목의 `local_progress_list`를 보면 repeat 전체 구간의 `(time, objValue)` progression을 볼 수 있음
- 제한: 내부 method별 분리 curve, repeat iteration 경계(`reps_001`, `reps_002`)는 현재 미지원
- 향후 확장: point에 현재 context 문자열(`reps_001.profile_fixed_ns`)을 추가하면 nested 경계 없이도 내부 method 분리가 가능

### 2026-04-05: 초기 구현 완료

- controller progression recorder 추가 (`controller_core.py`)
- objective point mirror 기록 (`add_obj_value_log`, `extend_obj_value_log` override)
- per-instance JSON 저장 (`hfs_single_instance_runner.py`)
- scenario 집계 helper (`method_progression_report.py`)
- multi-instance/multi-scenario runner 연동
- 테스트: `test_subroutine_progression_recorder.py` (5개), `test_method_progression_report.py` (13개)
- 전체 286 tests pass
